# Copy to .env.test and set values. Used when running tests (npm test).
# Tests use a separate database; run migrations on this DB before tests.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/serfix_test?schema=public"
JWT_SECRET=test-jwt-secret-min-32-characters-long
